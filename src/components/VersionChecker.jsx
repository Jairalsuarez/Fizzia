export function VersionChecker() {
  // Automatic version checks are disabled because they made tab restores feel
  // like app reloads. The update button in the menu can still reload manually.
  return null
}
